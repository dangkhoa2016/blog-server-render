const blogs_graphcms = require('./blogs_graphcms');
const blogs_repl = require('./blogs_repl');
const blogs_contentful = require('./blogs_contentful');

module.exports = {
    blogs_graphcms, blogs_repl, blogs_contentful
}
